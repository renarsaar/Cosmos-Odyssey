import { render, screen, fireEvent, waitFor } from '@testing-library/react';

import ImageMapArea from './index';

it('renders hint', () => {
  // const tipElement = screen.getByRole('tip');

  // expect(tipElement).toBeInTheDocument();
});
